# Travel Companion — MCP server env. Copy to .env (gitignored) on the VPS.
SUPABASE_URL=https://upvdcmjyyewgdvjcizbt.supabase.co   # required
SUPABASE_ANON_KEY=sb_publishable_...                    # required (publishable key)
PORT=4142                                               # optional
MCP_RATE_PER_MIN=60                                     # optional — per-token cap
# MCP_DEV_USER=dev-user-id   # DEV ONLY: bypass auth for local smoke tests (never set in prod)
